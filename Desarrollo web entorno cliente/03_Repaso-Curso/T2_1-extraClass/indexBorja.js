let operando1 = Number(prompt("introduce op1"));
let operando2 = Number(prompt("introduce op2"));

operSuma(operando1, operando2); // los argumentos son un array, y puedes iterar con for
// todo arguments minuto 7
function operSuma(param1, param2) {
  alert(
    `los operadores son ${param1}, ${param2} y la suma es ${param1 + param2}`,
  );
}
//todo lanzar exception en lugar de alert, el arguments <2>

//todo fotos encontrar imagen
let imagen = document.createElement("img");
imagen.src = "RUTA/$carta tipo al engaño de php";

//document.querySelector(#tapete).append(imagen)

// busca el id tapete y el nodo creado con create
