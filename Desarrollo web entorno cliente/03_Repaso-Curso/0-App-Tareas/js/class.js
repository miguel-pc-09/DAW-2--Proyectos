class Tarea {
  constructor(id, titulo, descripcion, fecha, prioridad) {
    ((this.id = id),
      (this.titulo = titulo),
      (this.descripcion = descripcion),
      (this.fecha = fecha),
      (this.prioridad = prioridad));
    /* this.completada=false
        this.imagen=  imagen */
  }
}
